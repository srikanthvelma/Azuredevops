# template for deploying to Azure App services 

steps:
  - task: AzureWebApp@1
    inputs:
      azureSubscription: 'nopwebapp'
      appType: 'webAppLinux'
      appName: 'srnop'
      package: '$(System.DefaultWorkingDirectory)/**/nopweb/*.zip'
      deploymentMethod: 'auto'


